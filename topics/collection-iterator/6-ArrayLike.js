/**
 * 类数组：key是有序数字的对象，{ '0': 'yuyongyu', '1': 'yuyy', '2': 'yyyy' }
 * @param item
 */
function test(...item) {
    return arguments;
}

test('yuyongyu', 'yuyy', 'yyyy');

/**
 * 1. for...
 */

for (let i){

}