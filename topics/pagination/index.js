class Pagination {
    constructor(pageNo = 1, pageSize = 10, totalCount = 0){
        this.pageNo = pageNo;
        this.pageSize = pageSize;
        this.totalCount = totalCount;
    }

    getPageNoList(){
        let pageNoList = [];
        let maxPageNo = Math.ceil(this.totalCount / this.pageSize);

        switch(this.pageNo){
            case this.
        }

    }
}